<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    android:id="@+id/cluster_icon_container"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/cluster_icon_title"
        android:padding="@dimen/marker_padding"
        android:text="just to check"
        android:textColor="@android:color/black"
        android:background="@drawable/image_border"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        />
    <ImageView
        android:layout_width="@dimen/marker_image"
        android:layout_height="@dimen/marker_image"
        android:padding="@dimen/marker_padding"
        android:id="@+id/cluster_icon_image"
        android:background="@drawable/image_border"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toBottomOf="@id/cluster_icon_title"/>
</androidx.constraintlayout.widget.ConstraintLayout>